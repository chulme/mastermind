The executable is inside the build directory and is named cw2.
The program has to be executed with sudo privileges:
	sudo build/cw2

Objects and the executable can be removed with the command:
	make clean
This command has to be executed inside the directory with the makefile.

Compilation can also be done with make:
	make all

